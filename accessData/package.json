{
  "private": true,
  "name": "@ecommerce/access_data",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "serve": "tsc",
    "lint": "eslint . --ext .ts"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --fix"
    ]
  },
  "peerDependencies": {
    "eslint-config-airbnb-base": "^14.2.0"
  },
  "dependencies": {
    "@types/knex": "^0.16.1",
    "knex": "^0.95.6",
    "pg": "^8.6.0",
    "typescript": "^4.3.2"
  }
}
